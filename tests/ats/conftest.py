"""Shared constants of the ATS scenarios: the API the chart renders and the
names of the cross-repo contract (the Harness admission label, the toolset
header)."""

API_VERSION = "kagent.dev/v1alpha3"
HARNESS_LABEL = "agent-platform.giantswarm.io/harness"
TOOLSET_HEADER = "X-Muster-Toolset"
